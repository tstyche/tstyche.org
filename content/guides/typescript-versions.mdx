import { Tip, Warning } from "@/components/Advice";

# TypeScript Versions

TSTyche has everything needed to verify that types are compatible across TypeScript versions.

## Supported Versions

Currently TypeScript `>=5.4` can be used for testing.

<Warning>

TypeScript 7 is not yet supported by TSTyche. Track progress in [#443](https://github.com/tstyche/tstyche/issues/443).

</Warning>

<Tip>

Use the [`--list`](../reference/command-line.mdx#list) command line option to inspect the list of supported versions.

</Tip>

## Version Compatibility Strategies

Projects that declare TypeScript compatibility mostly specify:

- a single version (only version `x.y` is supported)
- a range of versions (version `x.y` or above is supported)

### Single Version

<Warning>

**TS7 support.** When TypeScript 7 is installed, TSTyche will fall back to `6.0`.

</Warning>

If the `typescript` package is installed, TSTyche will load it. Otherwise, the latest version is used.

To test against a different version, pass it with the [`--target`](../reference/command-line.mdx#target) command line option or use the [`target`](../reference/config-file.mdx#target) configuration option.

The TypeScript version is shown in the output:

```ansi
[34muses[0m TypeScript 5.8.3[90m with ./tsconfig.json[0m
```

### Range of Versions

<Warning>

**TS7 support.** TypeScript 7 is not yet included in the ranges and `6.0` is the upper bound.
</Warning>

Keep in mind that TypeScript follows the Semantic Versioning specification only [partly](https://github.com/microsoft/TypeScript/wiki/TypeScript's-Release-Process). A patch release is still strictly only a bug fix, but a minor release may introduce breaking changes. This means an upgrade from `5.6` to `5.7` can break types.

For instance, if the lowest compatible TypeScript version is `5.6`, testing against all minor versions starting from that one is recommended:

```shell
tstyche --target '>=5.6'
```

This command will test using TypeScript `5.6`, `5.7`, `5.8` and so on. Newly released versions are added to the list as soon as they become available.

The report will include three (or more) versions of TypeScript:

```ansi
[34muses[0m TypeScript 5.6.3[90m with ./tsconfig.json[0m

[34muses[0m TypeScript 5.7.3[90m with ./tsconfig.json[0m

[34muses[0m TypeScript 5.8.3[90m with ./tsconfig.json[0m
```

To learn more about the version ranges, see the [Ranges](../reference/command-line.mdx#ranges) section in the documentation of the `--target` command line option.

<Tip>

Running tests against many versions of TypeScript is slow. Use ranges only in continuous integration workflows. If something fails, run TSTyche locally targeting that single version.

</Tip>

## Handling Changes

Changes between TypeScript versions can be handled using the [`// @tstyche if`](../reference/directives.mdx#if) comment directive:

```ts twoslash
import { expect, test } from "tstyche";

function isUint8Array(target: unknown): target is Uint8Array {
  return target instanceof Uint8Array;
}

test("isUint8Array", () => {
  const unknowns: Array<unknown> = [];

  // @tstyche if { target: ">=5.7" } -- Before TypeScript 5.7, 'Uint8Array' was not generic
  expect(unknowns.filter(isUint8Array)).type.toBe<Array<Uint8Array<ArrayBufferLike>>>();

  // @tstyche if { target: "<5.7" }
  expect(unknowns.filter(isUint8Array)).type.toBe<Array<Uint8Array>>();
});
```

If this directive is used at the top of the file, the whole test file will run only when the specified condition matches.

## Early Warnings

<Warning>

**TS7 support.** These tags resolve to TypeScript 7 and cannot be used. Track progress in [#443](https://github.com/tstyche/tstyche/issues/443).

</Warning>

TSTyche supports `beta`, `latest`, `next` and `rc` as the target tags. Use them as one-off commands or in continuous integration workflows.

To catch the breaking changes early, consider setting up a workflow that runs tests once a day against `typescript@next`. A GitHub Actions example is available in the [`tstyche/ts-nightly-example`](https://github.com/tstyche/ts-nightly-example) repository.

## The Store

The TSTyche store is a lightweight package manager that downloads the requested version of the `typescript` package. It keeps packages in a global cache. This means packages are shared across projects.

To know how to resolve tags like `latest` or `next`, every two hours the store fetches metadata of the `typescript` package from the registry.

These are the command line options dedicated to the store:

- [`--fetch`](../reference/command-line.mdx#fetch) downloads the specified TypeScript versions
- [`--list`](../reference/command-line.mdx#list) prints the list of supported TypeScript versions
- [`--prune`](../reference/command-line.mdx#prune) removes the whole cache directory
- [`--update`](../reference/command-line.mdx#update) fetches the latest metadata of the `typescript` package from the registry
